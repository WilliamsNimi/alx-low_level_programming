#include "main.h"
#include <stdio.h>
/**
 * mul- This is a multiplication function
 *
 * Description: It multiplies 2 numbers
 * @a: one of the numbers to be multiplied
 * @b: one of the numbers to be multiplied
 * Return: it returns the value of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
