/*
 * This is a c file that prints the text
 * Programming is ike building a multilingual puzzle
 * It uses the puts function instead of printf.
 * the return value is 0
 */
#include <stdio.h>
#include <string.h>

/**
 * This is the main function
 */
int main(void)
{
	char str1[] = "Programming is like building a multilingual puzzle";
	puts(str1);

	return (0);
}
