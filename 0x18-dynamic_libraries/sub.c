#include <stdio.h>
#include <stdlib.h>
#include "calc.h"
/**
 * sub- sub function
 * Description: subtracts 2 integers
 * @a: first interger param
 * @b: second integer param
 * Return: Returns the result as an int
 */
int sub(int a, int b)
{
	return (a - b);
}
